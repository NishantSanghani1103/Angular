import { InoutNumber } from './inout-number';

describe('InoutNumber', () => {
  it('should create an instance', () => {
    const directive = new InoutNumber();
    expect(directive).toBeTruthy();
  });
});
